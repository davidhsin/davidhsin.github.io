### Unix Command Memo

```bash
brew uses --installed <package> # 列出哪些套件依賴 package

brew deps --tree --installed <package> # 列出該 package 的相依套件
```

```bash
bear -- make # 輸出 compile_commands.json，解決 .c/c++ 檔 file not found 路徑問題
```
